# example_package

This is a fully fledged minimal example package that implements launch files, Generate Parameter Library, publishers, subscribers, and timers

Just replace `example_package` with the package name of your choice!